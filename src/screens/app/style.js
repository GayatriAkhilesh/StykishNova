import {StyleSheet} from 'react-native';

const style = (width, height) =>
  StyleSheet.create({
    main: {
      flex: 1,
    },
    title:{fontFamily: 'Poppins-SemiBold', fontSize: 22},
    
  });

export default style;
